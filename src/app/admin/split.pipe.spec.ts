/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SplitPipe } from './split.pipe';

describe('SplitPipe', () => {
  it('create an instance', () => {
    let pipe = new SplitPipe();
    expect(pipe).toBeTruthy();
  });
});
